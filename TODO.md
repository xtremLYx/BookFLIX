# TODO

- [x] Update `src/app/my-space/page.tsx` to fix book title overflow and keep title rendering consistent with cover thumbnail by tightening layout constraints (`min-w-0`, `w-full`) and applying `line-clamp`.
- [x] Run `npm run lint` and `npm run build` to ensure correctness.
- [ ] Verify UI: long titles should truncate within kanban cards; cover/title alignment should look consistent.



